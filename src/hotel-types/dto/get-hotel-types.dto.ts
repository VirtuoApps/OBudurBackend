// Simple DTO for getting hotel types
// No specific filters needed, just using general pagination
export class GetHotelTypesDto {
  // This can be extended in the future if specific filters are needed
}
